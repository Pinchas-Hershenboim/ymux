# Followups

Out-of-scope bugs found in passing. One line per item. Read at session start; surface P0/P1 to the user before starting new work.

Format:

```
- [ ] P<0|1|2|3> | <YYYY-MM-DD> | <file>:<line> | <one-line repro/symptom>
```

## Open

- [ ] P1 | 2026-08-20 | 322655e — app/src/terminalInstance.ts (flushPending, normaliseIncomingToLogical, applyRowDirections, bufferIsVisualOrder) + app/src/copyBidi.ts (visualToLogicalStream, ANSI_RE export) | **LOCAL HEBREW: BETTER DISPLAY vs TYPING LATENCY — A REAL TRADE-OFF, BOTH SIDES CONFIRMED LIVE BY YOSSI. THE COMMIT TO INVESTIGATE IS `322655e feat(rtl): normalise Claude's visual output to logical on the way in` (2026-08-20 16:53, the LAST commit on the zellij chain).** What it does, from its own commit body — the rule is three rows wide and only one row is the target:
    buffer   render          letters  placement
    visual   dir="rtl"       wrong    RIGHT
    visual   bidi-override   RIGHT    left      <- what shipped before it
    LOGICAL  dir="rtl"       RIGHT    RIGHT     <- 322655e, and what remote has always done
Placement can only come from the browser under `dir="rtl"`, and the browser only gets the letters right if handed LOGICAL text — so the stream is converted on the way in and a local pane stops being a special case. **LIVE EVIDENCE (2026-08-20, Yossi, real panes, three builds):** (1) merged 0.5.0 WITH 322655e — Hebrew in a local pane displayed *noticeably better* than any earlier build, and typing lagged; (2) main-only 7382de9 — no lag, Hebrew display visibly worse (it has none of the 18 RTL fixes and no `terminal.rtl` split at all); (3) branch build `ymux-rtl-test` (mtime 2026-08-19 16:32, end of `zellinj` at 37c6bc9, predates 322655e) — no lag. So the better display and the lag are the SAME commit. **COST:** `normaliseIncomingToLogical` = `bidiOwnedByTui && rtlModeRendered == "auto_per_line"`, and `default_local_rtl` ships `tui_owns_bidi: true`, so every local pane routes each rAF flush through `visualToLogicalStream(merged)`: an `RTL_RE` scan, `chunk.matchAll(ANSI_RE)` over CSI/OSC-dense text, then split/map/join per run. `!RTL_RE.test(chunk)` early-outs an all-Latin pane, which is why only Hebrew panes pay. It does NOT show up as a blocked main thread — zero `UI stall` warnings and one `longtask` across a 9-minute session — the cost is spread thinly across flushes. **CURRENT STATE ON YOSSI'S MACHINE:** `terminal.rtl.local.rtl_mode` set to `"off"` (WebGL verbatim), `tui_owns_bidi` left `true`. No lag, worse display. This is exactly the fallback the commit body names: "the fallback if the caret misbehaves is rtl_mode='off' — WebGL verbatim, which is precisely the old behaviour". Remote is untouched at `auto_per_line` / `tui_owns_bidi:false`. **TO REPRODUCE EITHER SIDE:** set `terminal.rtl.local.rtl_mode` to `auto_per_line` for the good-display-with-lag state, `off` for the no-lag state — one field, live, no rebuild (`ymux settings set --key terminal.rtl.local.rtl_mode --value auto_per_line`). **WHAT THE COMMIT ITSELF FLAGS AS UNTESTED:** "Not run live: the caret while typing Hebrew is the known risk and only a real pane can answer it." The latency is what a live run actually found; the caret question is still open and should be checked in the same session. **DIRECTIONS WORTH TRYING, none validated:** cache the RTL verdict per pane instead of re-scanning every chunk; narrow the normalise to panes actually running Claude rather than every local pane; make the reorder incremental rather than whole-chunk. Do NOT just revert 322655e — it is the only one of the three rows that gets letters AND placement right, and reverting returns local panes to "correct letters pinned to the left edge".
- [ ] P2 | 2026-08-20 | app/src-tauri/src/lib.rs (parse_zellij_sessions) | A ZELLIJ SESSION HAS NO `auto_name` AND NOBODY HAS LOOKED AT WHAT THE PICKER SHOWS. The merge of the zellij chain into 0.5.0 broke the build on exactly one line: `parse_zellij_sessions` is new on that branch, was written before Phase 81.F added the field, and so omitted it from the `TmuxSessionInfo` initializer. Patched as `auto_name: None`, which compiles and is honest — zellij sessions carry no session-meta join, so the picker falls back to the raw name the same way a pre-rename server does. What is NOT known: whether a zellij session that IS running Claude should get a name, and where from. tmux gets it from `~/.ymux/session-meta.json`, written by the CLI hooks; nothing wires that to zellij. Decide before the release notes claim session naming works everywhere. Found by `cargo check`, not by a test — there is no test covering the zellij picker's display fields.
- [ ] P2 | 2026-08-18 | app/src-tauri/resources/remote-manifest.json | Phase 81.F changed CLI source, so the Linux CLI binary's sha256 genuinely changes — this is the case Rule #12 carves out ("discard the churn UNLESS the embedded SHA actually changed"), and ci-windows run 32174489370 flagged the drift as expected. The committed manifest still claims `e50c2ceb…`. It can only be regenerated by a real `build-linux-cli.ps1` run on Windows, so it must be refreshed on the release cut that ships this — a hand-edited sha would be worse than a stale one, since sha256 IS the bootstrap comparison.
- [ ] P1 | 2026-08-18 | app/src-tauri/src/{lib.rs,rpc_server.rs}, app/src/PaneView.tsx | NOT LIVE-VERIFIED: the desktop half of Phase 81.F (session `auto_name`). It compiles and type-checks — ci-windows run 32174489370 is green on `cargo test --workspace` + `tsc` + frontend build — but nothing has RUN it. Owed on a real Windows box + server: pane header named from turn 1 (the `user-prompt-submit` arm now sets `auto_title` before its early return), picker primary line showing `<two words> · <date time>` with the raw `winmux-p_…` demoted below, `claude_title` reachable on hover, and a Hebrew prompt round-tripping without mojibake.
- [ ] P3 | 2026-08-18 | app/src/App.tsx:1058 | The OS window title reads only the manual pane `title`, never `auto_title` — so a Claude-named / auto-named pane falls through to the connection string in Alt+Tab, inconsistent with the pane header right next to it. Pre-existing Phase 81 oversight, widened in reach by 81.F (more panes now carry an auto name). One-line fix, but it changes what the taskbar shows, so it wants a deliberate call rather than a drive-by.
- [ ] P1 | 2026-08-18 | app/src-tauri/crates/winmux-core/src/lib.rs:41 (config_dir) | TWO WINMUX BUILDS SHARE %APPDATA%\winmux AND THE OLDER ONE SILENTLY STRIPS NEWER FIELDS. Hit live: winmux-beta3-MEGA-test.exe was running beside a dev build of the project-folders branch; it predates parent_id / is_project_root, serde ignores the unknown keys on load, and save_to_disk rewrites the WHOLE file from the struct — so every save by the old app erased the workspace nesting. Symptom is maximally misleading: pinned folders appear as separate top-level workspaces, worktrees stop listing, and NOTHING appears in the log of the new build, because it did nothing. Also fought over debug.log (alternating 4040/4060-byte saves are the two writers). Workaround that exists today: WINMUX_CONFIG_DIR isolates a build (`$env:WINMUX_CONFIG_DIR="$env:APPDATA\winmux-dev"`). Fix options: (a) refuse to start when another winmux holds the same config dir (lock file with the pid), or (b) stamp a schema version in workspaces.json and refuse to WRITE when the file's version is newer than the binary's — losing user data to an older build is the dangerous half. (b) is the real fix; (a) is the cheap one.
- [x] P0 | 2026-08-19 | app/src-tauri/src/tunnel_registry.rs (new), src/lib.rs (setup_workspace_reverse_tunnel / spawn_ssh / workspace_ensure_connected), cli/src/main.rs (rpc_call) | THE SUDDEN DISCONNECTS. FIXED by Phase 80.A-C, NOT VERIFIED LIVE. The original diagnosis in this entry was WRONG in its central claim and is corrected here rather than deleted, because the wrong version is the one someone will remember. It said the 20-vs-5 gap between `tcpip_forward got remote port` and `wrote ... last.env` was the cause. It is not: at 19:56:27 last.env was written CORRECTLY with 44495, and at 19:56:31 the same pane's hook still dialed 34173, the port from 73 minutes earlier. REAL ROOT CAUSE: a long-lived `claude` inside tmux inherits YMUX_SOCKET_ADDR at spawn and its hooks inherit it in turn; a reconnect allocates a fresh port; nothing can rewrite the environment of a running process; and cli/src/main.rs:1026 returns early from load_fallback_env_file whenever YMUX_SOCKET_ADDR is set, so on the one path where the file would help it is the one thing never read. Writing last.env more often fixes nothing — the port has to stop changing. FIXED BY: (a) sticky port — re-request the port the workspace last held, so the address baked into the running claude stays valid; desktop-only, works against every already-provisioned remote; (b) a per-workspace connect lock, ordered before bootstrap_guard::host_lock, so workspace_ensure_connected and spawn_ssh cannot allocate rival forwards (PaneView.smartConnect made that the NORMAL path — two tcpip_forwards 38ms apart, one watcher slot, loser orphaned, 4 server-side hygiene reaps); (c) TunnelLease, an RAII rollback for the four early exits that leaked a port + token + watcher each (three are `?` operators, hence RAII rather than manual); (d) TunnelRegistry replacing internal_reverse_tunnel_remote_ports (a HashSet sampled with .iter().next() that only ever grew — 20 dead ports) and workspace_tunnel_tokens (cleared nowhere in the codebase), so port/token/owning-session are one triple and try_ensure_port_watcher can no longer cross-pair them into -DENIED bad-mac; (e) the env write moved into setup_workspace_reverse_tunnel so every allocation updates it, with pane_id now Option and a read-modify-write that preserves the existing YMUX_PANE_ID — its ABSENCE makes the remote hook disable gating entirely, so blanking it would be worse than the original bug; (f) CLI-side, rpc_call retries once against a re-read last.env when its inherited address fails, bypassing the :1026 early-out. WHAT IS STILL OPEN: the whole thing is unverified against a real sshd. The specific unknowns are (1) whether sshd grants a re-requested specific port right after the previous listener closed — theoretically yes via SO_REUSEADDR on listen sockets, but Repro C settles it; (2) the russh trap that tcpip_forward returns Ok(0) for a NONZERO request, which request_reverse_forward is the single place that knows about — if that is wrong the tunnel dies silently with no error; (3) (f) is inert until every remote is re-bootstrapped with the new CLI. SMOKE (Repro C, the one that matters): connect a persistent tmux pane, start claude, let a hook succeed, note the port, force a reconnect WITHOUT touching the running claude, then type a prompt. Expect `reused sticky remote port <same port>` and no rpc-error-fallback. Invariant one-liner: `grep -c 'tcpip_forward got remote port'` must equal `grep -c 'wrote .*last\.env'`.
- [x] P2 | 2026-08-19 | app/src-tauri/crates/ymux-core/src/log_writer.rs (new), crates/ymux-core/src/lib.rs (write_line), src/log_sync.rs, cli/src/main.rs (hook_log) | TORN LOG LINES. FIXED by Phase 80.D, NOT VERIFIED LIVE. Root cause was exact: `writeln!` on an unbuffered std::fs::File goes through core::fmt::write, which issues the payload and the trailing "\n" as TWO write() syscalls; O_APPEND makes each atomic but not the pair, and there was no lock anywhere. Rotation was a stat-then-rename TOCTOU that could lose a whole 5MB generation, and on Windows a rename succeeds with our handle still open (FILE_SHARE_DELETE) so the writer kept appending into debug.log.1. log_sync amplified it by submitting up to 256KiB x 3 files x N hosts one line at a time. FIXED BY: a dedicated OS thread owning the handle, fed by a bounded std::sync::mpsc queue (8192, non-blocking, drops are reported as one counted WARN line); batches coalesced into a single write_all; rotation/prune/clear moved onto that thread and ordered against the lines around them; rotation closes the handle before renaming. Public signatures and the on-disk format are unchanged. cli hook_log got the same single-write_all fix — a separate process, so no queue is possible there, but log_sync copies its lines into debug.log verbatim. STILL OPEN: two ymux.exe instances would still race on rotation — tauri-plugin-single-instance is absent from app/src-tauri/Cargo.toml, see the separate entry below.
- [ ] P2 | 2026-08-19 | app/src-tauri/Cargo.toml (no tauri-plugin-single-instance) | TWO ymux.exe INSTANCES STILL RACE ON LOG ROTATION. Phase 80.D made debug.log writes single-owner WITHIN a process, and O_APPEND keeps a single write_all atomic even across processes, so lines from two instances interleave cleanly rather than tearing. Rotation does not: both would stat, both would rename, and one clobbers the other's debug.log.1. Out of scope for an in-process owner. This is the same class as the P2 above about two builds sharing %APPDATA%\ymux, and the same fix would cover both: a single-instance plugin, or a lock file naming the holding pid.
- [ ] P2 | 2026-08-19 | app/src-tauri/src/claude_usage.rs:262 | BACKOFF PATH LOGS ON EVERY REJECTION -> LOG FLOOD. `debug.flooded-2026-08-18.log` is 1.6MB of which 15,477 lines are the single message `[DEBUG] [USAGE] workspace=<id> backoff`. in_backoff() correctly suppresses the WORK but not the LOG, and the caller retries in a tight loop, so the throttle produces more I/O than the thing it throttles. Fix: log the entry into backoff once (with the until-timestamp) and stay silent while it holds, or rate-limit the line.

- [ ] P3 | 2026-08-19 | every `app/scripts/*.ps1` | **A NON-ASCII CHARACTER INSIDE A POWERSHELL STRING LITERAL BREAKS THE BUILD, SILENTLY AND CONFUSINGLY.** Windows PowerShell 5.1 reads a BOM-less UTF-8 file as ANSI. A UTF-8 em-dash (`E2 80 94`) decodes to three CP1252 chars whose last one is U+201D, a smart quote the PowerShell tokenizer accepts as a string delimiter -- so `Write-Host "... - left in place"` with a real em-dash terminates the string in the wrong place and the parser then reports a cascade of `Missing closing '}'` errors pointing at innocent lines 10-100 lines away. Hit while editing `build-linux-cli.ps1`; the existing em-dashes in that file are all inside `#` comments, which is why it had never bitten. **GUARDED 2026-08-19:** `ci-windows.yml` now runs a "Parse-check the build scripts" step (`shell: powershell`, i.e. 5.1) over `app/scripts/*.ps1` and fails on any parse error, in seconds, before anything expensive runs. Left open as P3 because the guard catches it rather than preventing it -- writing the scripts with a UTF-8 BOM, which is what WinPS 5.1 actually wants, would.
- [ ] P2 | 2026-08-18 | app/src-tauri/src/workspaces_merge.rs | TWO YMUX BUILDS SHARING %APPDATA%\ymux — was P1, now largely handled. Hit live: winmux-beta3-MEGA-test.exe ran beside a dev build; it predates parent_id / is_project_root, serde ignored the unknown keys and save_to_disk rewrote the WHOLE file without them, erasing the workspace nesting with nothing in the new build's log. FIXED FORWARD by a three-way merge on every save: re-read the file, and if it moved since we last touched it apply only our delta on top of theirs. Merging at the JSON level (not over WorkspacesFile) means fields a binary has never heard of survive its own save, which is the actual class of bug. WHAT IS STILL OPEN: (1) an already-shipped older binary has none of this, so it will keep stripping until it is replaced — YMUX_CONFIG_DIR isolation is the only defence there; (2) the merge path has NOT run in a real app — an idle winmux never saves, so it is covered by 12 tests at the `reconcile` seam rather than live; (3) a lock file naming the holding pid would still be worth having, to say out loud that two instances share one config instead of silently reconciling behind the user's back.
- [ ] P2 | 2026-08-18 | app/scripts/build-linux-cli.ps1 (RUSTFLAGS scrub) + .github/workflows/ci-windows.yml (Report staged-resource drift) | THE `ymux-linux-x64` CI BUILDS IS NOT THE ONE COMMITTED, AND THE ALARM FOR IT IS ALWAYS ON. CI's fresh build came out 2,498,992 bytes / sha fbf2c01c… against the committed 2,521,136 / 8ecb0623… — a 22KB difference, i.e. a different compiler, not a path string. Local rustc here is 1.95.0; CI takes `dtolnay/rust-toolchain@stable`. PRE-EXISTING, not the rename: run 32178442186 (the merge commit, before any of this work) emitted the same `##[notice]remote-manifest sha256 changed`. Nothing breaks at runtime — a release regenerates the binary and remote-manifest.json together, so the sha the bootstrap compares against is always self-consistent. Two real costs though: build-linux-cli.ps1's header claims the RUSTFLAGS scrub makes the output "byte-identical regardless of who built it", which is not true across toolchain versions; and the CI notice fires on EVERY run, so it has stopped being a signal — it was written to flag the case where the staged CLI actually moved. Fix is one of: pin the toolchain (rust-toolchain.toml, so CI and Yossi's box agree), or stop committing the binary and let the release build produce it. **UPDATE 2026-08-19:** the *second* cause is fixed — `cli/build.rs` baked `SystemTime::now()` into `YMUX_BUILD_TIME`, so two builds of identical source on the SAME machine also disagreed; it is the commit time now. The cross-toolchain 22KB delta is untouched and still needs `rust-toolchain.toml`. The always-on CI notice should also quieten down: `build-linux-cli.ps1` no longer rewrites `remote-manifest.json` (or restages a binary) when the sha256 hasn't moved, so the `git diff` drift step now fires only on a real change.
- [ ] P2 | 2026-08-20 | app/src/terminalInstance.ts (contextmenu handler ~:917, showTerminalContextMenu ~:464) | **RIGHT-CLICK Copy stopped working on a local zellij pane after `mouse_mode true`.** Reported by Yossi in the live pass: wheel scroll works, Shift+drag copies, Ctrl+C copies — right-click does not. Not a regression in the copy plumbing (all four paths share `copyToClipboard` now and the other three work); it is a consequence of handing zellij the mouse, and there are two independent causes, both plausible and both needing a live check before fixing: (1) with mouse reporting on, xterm.js forwards the right-click to zellij as an SGR event and our `contextmenu` listener may never fire, so the menu never opens; (2) even if it opens, `showTerminalContextMenu` builds its Copy item from `ti.term.getSelection()`, which is EMPTY when the selection belongs to zellij rather than to xterm.js — so the item renders disabled (`!!sel`). Shift bypasses zellij's capture, which is exactly why the Shift paths are the ones that work. Fix options, in rough order of cost: require Shift+right-click and say so in the menu; or have the menu fall back to the last OSC 52 payload when xterm has no selection; or ask zellij for the selection. Deprioritised by Yossi — "less important for now".
- [ ] P1 | 2026-08-20 | app/src-tauri/src/lib.rs (sanitize_tmux_session_name_for_title ~1616, effective_tmux_name ~6723, build_zellij_attach_command ~2374) | **A PANE TITLE CAN BREAK ITS OWN SESSION NAME, and it may be part of "kill did nothing".** `build_zellij_attach_command` claims in a comment that "session names come from `sanitize_session_name`, which emits only `[A-Za-z0-9_-]`, so there is nothing here to quote or escape". No function has that name, and the claim is FALSE for the title path: `effective_tmux_name` prefers `sanitize_tmux_session_name_for_title`, which substitutes only `.`, `:` and whitespace. So `;`, `&`, `|`, `$`, backticks and quotes survive into `format!("zellij attach -c {name}

")`, which is typed VERBATIM into cmd.exe or PowerShell — where `;` is a statement separator. Self-inflicted (the user's own title, their own shell), so not a security emergency, but the mundane consequence bites regardless: zellij creates the session under a different name than ymux stored, so `list-sessions`, kill and restore all silently fail to match it. The test that should have caught it, `zellij_attach_command_is_a_single_safe_line`, only ever exercises the already-safe `ymux-p_1a2b_0`. Fix sketch: a `sanitize_zellij_session_name` reducing to `[A-Za-z0-9_-]`, applied ONLY when deriving a NEW name, never when attaching to one the user picked from the list. NOT done with the 2026-08-20 lock work because changing name derivation strands every existing session whose name holds punctuation or Unicode — it needs its own migration and its own live test.
- [x] P2 | 2026-08-20 | app/src/copyBidi.ts + app/src/terminalInstance.ts (copyToClipboard) | **DONE 2026-08-20. Hebrew copied out of a pane pasted REVERSED.** Measured by Yossi, and the two panes were exactly inverted: plain PowerShell displayed reversed but pasted CORRECTLY (its buffer is logical), while Claude Code displayed correctly but pasted reversed (its buffer is visual, and `unicode-bidi: bidi-override` renders visual bytes verbatim, which is why the screen looked right). Fixed with a visual->logical pass on the way to the clipboard, gated per pane on the Claude-in-front signal, routed through one `copyToClipboard` that all four copy paths now share. **Two claims in the original entry were WRONG and are corrected here:** (1) it said `mouse_mode false` was 'the precondition that makes it reachable at all: with zellij owning the mouse it wrote to the clipboard over OSC52 and ymux never saw the text' — false, `ClipboardAddon` has always routed OSC 52 through our own provider, so the mouse setting was never a blocker and the wheel could come back for free; (2) it framed visual order as a property of local/ConPTY panes generally — it is a property of Claude Code, not of the shell. Residual, and NOT fixable: two different logical strings can share one visual form (`"שלום עולם 123 abc"` and `"שלום עולם abc 123"` both render to `"abc 123 םלוע םולש"`), so a selection with adjacent digits and Latin at an RTL edge picks one reading. Pinned as a test.
- [ ] P3 | 2026-08-20 | FOLLOWUPS.md:33 and :38 | The same entry is filed twice — one spelling `winmux-tmux.conf`, the other `ymux-tmux.conf`. A leftover of the rename. Delete the stale one.

- [ ] P2 | 2026-08-19 | app/src/terminalInstance.ts (renderer fork, ~line 791) | A LOCAL Windows pane renders Hebrew REVERSED under `rtl_mode="auto_per_line"`, even though the stream reaching xterm there is logical order (proved: `bidi_reorder`, which does logical→visual, renders it correctly on the same pane). auto_per_line uses the DOM renderer + per-row `dir`, so the browser should reorder it correctly — the same setting works on an SSH pane. Something on the local path is not getting the per-row dir treatment, most likely the pane being built with the WebGL renderer (`rtlModeAtConstruct` frozen from a different mode at construction). NOT diagnosed, and deliberately not guessed at: the next step is a log line stating, per pane, which renderer was chosen and what `rtlModeAtConstruct` was, then read `%APPDATA%\ymux\debug.log`. **2026-08-19 (later): that log line now EXISTS** — `rtl-pane` at construction (profile / live mode / rtlModeAtConstruct / renderer actually loaded / policy / staleRenderer) and `rtl-dirs` per direction pass. Reproduce and read those two lines; the diagnosis should fall out of them rather than out of a screenshot. Worked around, not fixed, by the local/remote profile split (`0b40012`): local defaults to `bidi_reorder`, which is correct there. Repro: local PowerShell pane, rtl_mode=auto_per_line, restart the app, `echo "שלום"` → shows `םולש`. **2026-08-20, much stronger evidence:** Yossi's side-by-side shows the two panes are exactly inverted — plain PowerShell displays reversed but pastes CORRECTLY, Claude Code displays correctly but pasted reversed. The clipboard half is now fixed (see the 2026-08-20 entry above), which isolates this one precisely: PowerShell's buffer is LOGICAL (proved by its copy being right), so this is purely a rendering bug — auto_per_line's DOM renderer plus a per-row `dir` should lay logical Hebrew out correctly and does not. Same visual/logical model as the copy bug, opposite sign; they are siblings.
- [ ] P2 | 2026-08-19 | app/src/App.tsx (connectPane + the feed:item-added listener) | **Re-attaching to a persistent session where Claude never stopped starts in the WRONG bidi state until the first hook fires.** Measured from Yossi's debug.log: panes with Claude running in them reported `tui=0`, because a reattach produces no `mode=claude` (the wizard was not used) and no `session-start` (Claude never started — that is what persistence means), and the title never matches (`title-seen ... match=0 len=57`, zellij's own). Mitigated, not fixed: ANY Claude hook now sets the signal, and `stop` lands after every reply, so the state corrects itself on the first interaction. The residue is the window between reattach and that first hook — the exact moment a user opens the app and looks at Claude — plus the case where the ymux Claude hooks are not installed at all, where nothing ever fires. Real fix is to PERSIST the flag with the pane (workspaces.json, `#[serde(default)]`), so a persistent session carries 'Claude is running in me' the way it carries everything else. Trade-off to write down when doing it: a stale true (Claude died while the app was closed) makes SHELL Hebrew render reversed until a hook or a title corrects it — the same failure class, inverted.
- [ ] P1 | 2026-08-19 | app/src-tauri/cli/src/hooks.rs (is_ymux_entry / the skip path) | **`setup-hooks` decides an entry is 'already present' by NAME and never checks that the command still exists, so the winmux -> ymux rename left a permanently dead hook.** From Yossi's Claude pane, verbatim: `Stop hook error: /usr/bin/bash: line 1: C:\Users\mlastudent371\winmux-cli.exe: No such file or directory`. His ~/.claude/settings.json references BOTH spellings; `is_ymux_entry` correctly matches the old one as ours, and the installer then reports `already present (would skip)` and leaves the dead path in place. Verified here: `--dry-run` skips all four, `--force` rewrites all four with the live path and takes a timestamped .bak. Two costs, and the second is the expensive one: the hook fires and errors on EVERY Claude stop, and it silently disabled the hook-based half of the per-pane 'Claude is in front' signal that local RTL depends on -- which is why local hooks never reached the app all session while remote ones did, and why that looked like a detection bug for several rounds. Fix: in the skip path, resolve the entry's executable and treat a missing file as needing replacement, so 'already present' means 'present AND runnable'. Workaround until then: re-run with `--force`.
- [ ] P1 | 2026-08-19 | app/src/textDirection.ts (nextTuiOwnsBidi) + app/src/terminalInstance.ts (onTitleChanged) | **`tui_owns_bidi` IS DEAD CODE IN PRACTICE — the detector never fires.** Proved from Yossi's debug.log on the build that added the RTL diagnostics: every `rtl-dirs` line reports `tui=0`, on every pane, both profiles, and the whole file contains not one `tui-owns-bidi` transition. The detection reads the terminal TITLE (OSC 0/2), and inside zellij the title reaching ymux is zellij's own — Claude's never gets through. Over SSH+tmux it did not fire either. So the Settings checkbox promises behaviour that cannot happen, and the dynamic "no bidi while a self-reordering TUI holds the pane" switch has never actually run. Worked around, not fixed: local now defaults to `rtl_mode="off"`, which needs no detection — at the cost of shell Hebrew (logical order) rendering reversed on a local pane. The fix is a signal that survives a multiplexer: **the ymux Claude hooks already run per pane with `YMUX_PANE_ID` set** and could report Claude's start/stop directly, which is both more reliable than a title and works over SSH too. Until then, do not add features that depend on `tuiOwnsBidi` being true.
- [ ] P2 | 2026-08-19 | app/src/bidi.ts + app/src/terminalInstance.ts (flushPending) | Under `rtl_mode="bidi_reorder"` the caret stays pinned to the right instead of tracking the text, and a partial repaint can leave a line half-reordered ("part rewritten correctly and part in the middle rewritten again"). Both are inherent to transforming the byte stream for a TUI that repaints regions and positions its cursor in columns of the UNtransformed text — two coordinate systems that diverge. This is what terminal-wg calls "the cursed cursor"; no xterm.js-based terminal has solved it. A real fix needs whole-line reassembly before reordering plus cursor tracking through the transform, and is its own piece of work — not a follow-up to squeeze into an RTL bugfix. See docs/DECISIONS.md, the 2026-08-19 entries.
- [ ] P2 | 2026-08-19 | app/src-tauri/crates/ymux-core/src/lib.rs (config_dir + write_line) | `cargo test` WRITES INTO THE USER'S REAL `%APPDATA%\ymux\debug.log`. Proof: while diagnosing a live RTL run I read the log tail and found the newest 41 lines were TICKETS parse warnings and four repeats of "migrated 1 WSL connection(s) to local" — those are unit tests, not an app run. `config_dir()` honours `YMUX_CONFIG_DIR`, so the tests CAN be isolated; most of them just don't set it. Two costs: the user's support log gets salted with test noise, and it burns real diagnostic time (it sent me looking for an app boot that had never happened). Fix: set `YMUX_CONFIG_DIR` to a temp dir for the whole test binary (a `#[ctor]`-style once, or a test-only default in `config_dir` gated on `cfg!(test)`).

- [ ] P2 | 2026-08-18 | app/scripts/build-linux-cli.ps1 (RUSTFLAGS scrub) + .github/workflows/ci-windows.yml (Report staged-resource drift) | THE `ymux-linux-x64` CI BUILDS IS NOT THE ONE COMMITTED, AND THE ALARM FOR IT IS ALWAYS ON. CI's fresh build came out 2,498,992 bytes / sha fbf2c01c… against the committed 2,521,136 / 8ecb0623… — a 22KB difference, i.e. a different compiler, not a path string. Local rustc here is 1.95.0; CI takes `dtolnay/rust-toolchain@stable`. PRE-EXISTING, not the rename: run 32178442186 (the merge commit, before any of this work) emitted the same `##[notice]remote-manifest sha256 changed`. Nothing breaks at runtime — a release regenerates the binary and remote-manifest.json together, so the sha the bootstrap compares against is always self-consistent. Two real costs though: build-linux-cli.ps1's header claims the RUSTFLAGS scrub makes the output "byte-identical regardless of who built it", which is not true across toolchain versions; and the CI notice fires on EVERY run, so it has stopped being a signal — it was written to flag the case where the staged CLI actually moved. Fix is one of: pin the toolchain (rust-toolchain.toml, so CI and Yossi's box agree), or stop committing the binary and let the release build produce it.
- [ ] P2 | 2026-08-18 | sdk-gen/specs/openapi.json, sdk/typescript/src/types.gen.ts | THE RENAME COMMIT ALSO CARRIES SDK DRIFT THAT PREDATES IT. Running `sdk-gen/ci-check.mjs` regenerated the specs and found ~400 lines of openapi.json and ~270 of types.gen.ts that were already stale — new components (`HookDecisionRequest`, `HookForwardRequest`) from an earlier server change where nobody ran `npm run gen`. The regenerated output is by definition correct, so it was committed rather than left stale, but it is NOT mine and reviewing the rename diff will show it as unexplained. Root cause worth fixing: `ci-windows.yml` does not run the drift guard, which is why it went unnoticed — add a `node sdk-gen/ci-check.mjs` step and this class of surprise stops recurring.
- [ ] P2 | 2026-08-18 | build-and-verify.ps1:23,63 | LEFT ON THE OLD NAMES ON PURPOSE — CLAUDE.md Rule #11 puts repo-root .ps1 helpers off limits. The rename sweep did touch it and was reverted, which was right twice over: both strings name things that really are still called winmux on Yossi's disk (the worktree `winmux-wt-lh-insights` and `C:\Users\mlastudent371\winmux-beta3-lh-insights-test.exe`), so renaming them inside the script would have BROKEN it rather than updated it. Yossi's to change, if and when he renames those two on disk. `app/scripts/*.ps1` are NOT in this category — they are project build scripts and were renamed normally.
- [ ] P0 | 2026-08-18 | app/src-tauri/tauri.conf.json:5 | THE RENAME RELEASE MUST BE CUT AS 0.5.0, NOT 0.4.6. `identifier` moved `com.winmux.app` → `com.ymux.app`, which is the MSI/NSIS upgrade key: Windows treats the new package as a DIFFERENT product, so it installs SIDE BY SIDE and the old winmux stays in Programs and Features with its own Start Menu entry. There is no shim for this — the identifier IS the identity. Data is safe (ymux-core `config_dir` renames `%APPDATA%\winmux` → `%APPDATA%\ymux` on first boot), but the release notes must tell users to uninstall the old winmux AFTER the first successful ymux launch, in that order — uninstalling first is harmless, uninstalling while both are installed is what the migration expects, and running BOTH at once hits the P1 shared-config-dir corruption above with the extra twist that they now use different directories.
- [ ] P1 | 2026-08-18 | crates/ymux-tunnel/src/lib.rs (CHALLENGE_TAG), server/internal/chat/chat_hookrpc.go (challengeTag) | RETIRE THE RENAME SHIMS ONE RELEASE AFTER 0.5.0. The handshake still EMITS the legacy `WINMUX-CHALLENGE` tag, because a pre-rename `winmux-linux-x64` does a literal prefix match and hangs up on anything else — so until every provisioned remote has been re-bootstrapped, ymux↔ymux traffic is still speaking winmux on the wire. Both ends already read and mirror either dialect (covered by TestHookHandshakeAcceptsBothDialects / TestHookHandshakeDeniesInClientDialect), so the flip is a one-line change on each side — do them together. Same release should drop: the `WINMUX_*` dual-write (lib.rs spawn_local_pty + the tmux set-environment loop, ymux-tunnel write_remote_env_file, Go spawnEnv), the CLI's `adopt_legacy_env` + `migrate_legacy_home_dir`, the legacy named-pipe listener (rpc_server.rs LEGACY_LISTENER_POOL_SIZE + ymux-core `pipe_name_legacy`), `X-Winmux-Truncated`, and the `use_winmux_tmux_config` serde alias. Keep the `%APPDATA%` and `~/.winmux` migrations longer — those cost nothing and a user can upgrade from any age.
- [ ] P1 | 2026-08-18 | whole rename, no single file | NOTHING ABOUT THIS RENAME HAS BEEN VERIFIED LIVE (Rule #14). Green: cargo check + cargo test, go vet + go test (incl. 2 new dual-dialect handshake tests), tsc, vite build, and all three embedded binaries rebaked from renamed sources. NOT run: the app itself. Smoke list, in order, and each one exercises a shim that only exists on the upgrade path: (1) launch with an existing `%APPDATA%\winmux` present and confirm the log line `config dir migrated … (winmux -> ymux rename)` and that workspaces/settings/keys all survived; (2) connect to a server provisioned by the OLD build and confirm `bootstrap: migrated remote …` plus a working pane — this is the `~/.winmux` → `~/.ymux` copy AND the legacy handshake tag together; (3) fire a Claude hook in that pane and confirm the permission card appears (proves env promotion + `is_ymux_entry` replacing rather than duplicating the old hook — if it DUPLICATES you get two cards per tool call); (4) Add-ons → Insights on a host with the old daemon: detect must report the existing install, and after Update `systemctl --user list-units | grep -c mux` must be 1, not 2; (5) `ymux list-workspaces` from a shell, then the same with a pre-rename `winmux-cli.exe` if one is still on PATH (proves the legacy pipe listener).
- [ ] P3 | 2026-08-18 | app/src/PaneView.tsx:1794 | The new-connection wizard renders its OWN copy of the remote directory list (`.nc-browse-*` / `.nc-dir-item`, ncView()==="browse") — same `file_list_remote` call, same navigation, different chrome. The standalone copy was extracted to app/src/DirPicker.tsx (it had been dead code: its guard `dirPicker() && !dirPickForNewConn()` could never be true); the wizard copy was left alone deliberately, since folding it in means touching the connect flow. Two implementations of one browser will drift. Fix: give DirPicker an `inline` variant and have the wizard render it.
- [ ] P1 | 2026-08-18 | app/src/Sidebar.tsx + app/src-tauri/src/lib.rs | NOT VERIFIED LIVE: project folders v4 (a project folder IS a child workspace) and the pane-header overflow menu. Supersedes the 2026-08-16 v2 entry, whose model no longer exists. Smoke: (1) split to 6-8 panes, shrink the window, buttons move into the chevron menu and the menu OPENS - check Hebrew too; (2) right-click an SSH workspace -> pin -> the SFTP browser lists REAL remote dirs -> pick a repo -> a child row appears nested under it with a folder glyph; (3) select it, open a pane -> the wizard shows the folder read-only, TMUX/regular, and a session list containing ONLY that folder's sessions -> connect -> `pwd` is the folder (this exercises the SSH-cwd injection); (4) expand its worktrees -> open one -> nests under the folder, `pwd` is the worktree, and the repo root's own entry does NOT appear as a stub; (5) + -> new worktree -> its workspace opens AND SURVIVES (the cmux#5032 trap); (6) delete the parent -> the confirm NAMES the descendants and the live count -> all of them go, directories on the host untouched; (7) disconnected host -> clear error, no stuck spinner; non-repo path -> git's message, nothing created; (8) restart -> nesting + collapse state persist; (9) MIGRATION: open a workspaces.json still carrying `project_folders` and confirm the pins reappear as child workspaces and the worktree workspaces re-parent under them.
- [ ] P0 | 2026-08-18 | server-side, no known code path | tmux sessions vanish from the SERVER after an abrupt client loss (laptop unplugged from AC). Yossi confirms `tmux ls` was already empty BEFORE his manual reboot, so the reboot is not the cause. NO MECHANISM FOUND: every kill path in the repo was read and excluded — the 4 `tmux kill-session` sites (all behind explicit user action: PaneView.tsx:951 -> App.tsx:1655 -> lib.rs:6524, and rpc_server.rs:1745 via `ymux pane-disconnect --kill`), `kill_session_inner` (lib.rs:3288, explicitly preserves tmux), the bootstrap `pkill` (tmux's cmdline is `tmux -f $HOME/.ymux/tmux.conf new-session…`, which never matched the pattern), the port-watch reaper (scoped to `port-watch [-]-workspace X`), the addons pkill (scoped to ymux-server/insights), and the Go hygiene daemon (server/internal/insights/hygiene.go: `killPids` permits ONLY duplicate port-watchers by PID; orphan claude needs etime > 24h and is never auto-killed). No `destroy-unattached`/`exit-empty` in the shipped conf. Two candidates left, both needing evidence rather than code reading: (a) something outside ymux — OOM killer on a shared box that was sitting at load average 22 with 10 users; (b) the `tmux ls` looked at a different socket (other user, other `TMUX_TMPDIR`) while the sessions were alive. NEXT STEP is data, not a fix: `journalctl -k --since '-7d' | grep -iE 'oom|killed process'`, `journalctl --list-boots`, and `id -un; echo $TMUX_TMPDIR; ls -la /tmp/tmux-$(id -u)/`. The new `tmux-inventory:` log line (lib.rs, before each attach) timestamps the disappearance from our side. Do NOT ship a fix on a guess — three hypotheses have already been falsified (systemd-logind KillUserProcesses, an external reboot, and any in-repo kill path).
- [ ] P1 | 2026-08-18 | app/src/App.tsx (reconnect) | NOT VERIFIED LIVE: the per-pane reconnect fix. Compiles + tsc clean, but the decisive test is 4 panes on one workspace surviving a single full network drop (all four must retry and reattach; before the fix three were abandoned without a single attempt). There is no JS test runner in this repo, so this cannot be covered by a unit test without adding one — it needs a real build and a real drop.
- [ ] P1 | 2026-08-18 | app/src-tauri/src/bootstrap_guard.rs | NOT VERIFIED LIVE: bootstrap dedup + negative cache + skew banner. Unit-tested (6 tests) but never run against a real host. Repro that matters: build from main (manifest e50c2ceb…) against a server holding b3a3974…, open 3-4 panes at once, expect EXACTLY ONE SFTP stream in debug.log, one `bootstrap: COMPLETE — upload verified`, and no `.tmp` left in ~/.ymux/bin.
- [ ] P2 | 2026-08-18 | remote server ~/.ymux/bin | Stale temp files left by the OLD code on Yossi's server: `ymux-linux-x64.tmp` (522,240 bytes = exactly 2 x russh-sftp's 261,120 chunk) and `ymux-server.tmp` (0 bytes). Nothing cleans these up — the new code removes its own partials but has no sweeper for pre-existing ones. Needs Yossi's explicit go-ahead to delete on his box.
- [ ] P2 | 2026-08-11 | app/src-tauri/src/lib.rs:1832 | The tmux attach script appends `\; set -g mouse on`, which directly contradicts `set -g mouse off` in resources/ymux-tmux.conf:28. That conf line is deliberate and its comment says why: with mouse on, tmux captures every click via xterm mouse-tracking, so drag-to-select and right-click-to-paste stop working inside tmux panes. Phase 65 (bug EE) turned it back on via the command chain to get tmux-native wheel scrollback, because `mouse on` IN the conf garbled Claude Code's output. Net effect today: mouse is on, so selection/paste-by-mouse inside tmux is degraded on BOTH ssh and wsl panes, for scrollback. Needs a decision, not a flip — possibly per-pane or tied to whether an alt-screen app is running.
- [ ] P2 | 2026-08-11, re-diagnosed 2026-08-18 | app/src/terminalInstance.ts (~17 sites) | Raw `console.*` throughout, against Rule #9 (frontend must use `createLogger`). This is how the clipboard-read failure stayed invisible: the only handler was `console.warn("terminal paste failed")`, which never reaches %APPDATA%\winmux\debug.log. **CORRECTION (2026-08-18): this is NOT "pre-existing debt" as originally logged — it is a regression from merge `bcaa330` (2026-07-31), the same merge that silently dropped tuiOwnsBidi.** Counts by ref for `terminalInstance.ts`: `456f0ed` = 24 logger sites / 0 `console.*`; the other merge parent `45b6592` = 0 / 17; the merge result and `main` ever since = 0 / 17. Phase 79 DID convert this file; the merge resolution threw the conversion away. Restoring it is mechanical (re-apply the `456f0ed` version of those call sites onto current HEAD, keeping the newer clipboard/`diag_log` work) — deliberately NOT bundled into the tuiOwnsBidi restore to keep that diff reviewable. Raised to P2: Rule #9 says the unified logger is user-visible, so 17 blind sites is a support problem, not cosmetics.
- [ ] P1 | 2026-08-12 | app/src-tauri/src/rpc_server.rs (feed.push) | NOT VERIFIED LIVE: Stop-pulse fix. Hook feed items now derive workspace_id from pane_id, which is the prerequisite for the sidebar amber pulse (App.tsx activeHookWorkspaceIds) to fire on Stop. Needs a real ymux pane running Claude + a build of this branch to confirm the row actually pulses. The pane-BORDER pulse (OSC 9/99/777 from Claude Code's bell) is a separate path and was NOT touched — if that's the "blink" Yossi means, this fix won't address it.
- [ ] P3 | 2026-08-11 | app/src/terminalInstance.ts (18 sites) | Raw `console.*` throughout, against Rule #9 (frontend must use `createLogger`). This is how the clipboard-read failure stayed invisible: the only handler was `console.warn("terminal paste failed")`, which never reaches %APPDATA%\ymux\debug.log. The two clipboard paths were converted with the paste fix; the other ~16 (theme/font apply, WebGL fallback, pty_write, resetTerminal, OSC52) are untouched pre-existing debt.
- [ ] P3 | 2026-08-12 | app/src-tauri/src/rpc_server.rs (Decision::Auto) | Removed the per-Auto audit feed item (was flooding the feed with 300 "✓ matched no risky pattern" dots). If an audit trail of auto-approvals is ever wanted, it belongs in a dedicated filtered/collapsed view, NOT the live notification feed. debug.log already records each decision.
- [ ] P1 | 2026-08-03 | app/src-tauri/src/fonts.rs | Install has no matching UNINSTALL. A user who installs FiraCode Nerd Font (27 MB, 6 files) has no in-app way to remove it — they must hand-delete from %LOCALAPPDATA%\Microsoft\Windows\Fonts and HKCU. Symmetry matters here because we are the ones who put the files there.
- [ ] P2 | 2026-08-03 | app/src-tauri/src/local_setup.rs:881 | WSL elevation detection is an English substring match (`low.contains("elevat") || "0x80070005"`) -> on localized Windows a UAC-declined `wsl --install` is misreported as a generic StepFailed. Also no ERROR_SUCCESS_REBOOT_REQUIRED (3010) / reboot-pending concept in inspect_wsl (:378-473).
- [ ] P2 | 2026-08-03 | app/src-tauri/src/fonts.rs:73 | Font catalog pins upstream tags+sha256; needs a periodic refresh check (JetBrainsMono v2.304, FiraCode 6.2, nerd-fonts v3.5.0, powerlevel10k-media master). An upstream re-release makes install fail with "checksum mismatch" — correct but opaque to the user.
- [ ] P2 | 2026-08-03 | app/src-tauri/src/fonts.rs:588 | MesloLGS NF assets are pinned to `master` on powerlevel10k-media, not a tag — the sha256 pin is what protects us, but a legit upstream update will break the entry until refreshed. Prefer a tagged source if one appears.
- [ ] P3 | 2026-08-03 | app/src-tauri/src/lib.rs:1604 | bidi_filter state is keyed by pane, so an SSH channel's stdout and stderr still share one escape-sequence state machine (the UTF-8 + OSC halves were split by the emit_data fix). A partial CSI/OSC on one stream can be completed by the other's bytes. Deliberate: a synthetic per-stream key would miss the per-pane smart_bidi toggle and silently leave stderr unfiltered. Only reachable via ExtendedData, rare on a PTY channel.
- [ ] P3 | 2026-08-03 | app/src-tauri/src/settings.rs:1662 | Mono/UI split is a name-substring heuristic; "Courier 10,12,15 (120)" bitmap .fon entries still reach the terminal picker and are known to measure badly (see terminalInstance.ts remeasureFont comment). Consider filtering bitmap .fon families out entirely.
- [ ] P3 | 2026-08-03 | app/src-tauri/src/settings.rs:1629 | Font enumeration shells out to PowerShell on every Settings open (~300ms). Fine at current call frequency; if the picker ever refreshes more often, read the registry via the winreg dep that fonts.rs now pulls in.
- [ ] P3 | 2026-08-03 | app/src-tauri/build.rs (resources) | A fresh git worktree can't `cargo build`: `app/src-tauri/resources/ymux-cli.exe` is gitignored, so the Tauri build script dies with "resource path doesn't exist". Has to be copied from the main checkout by hand. Either build it on demand or document the step.


- [ ] P2 | 2026-08-12 | app/src-tauri/src/diff_pane.rs:39 | The Diff pane is silently broken for every non-Local workspace. `fetch_diff` runs `git -C <cwd>` with `tokio::process::Command` on the WINDOWS host and `lookup_pane_context` (:74) never reads `ws.connection`. For an SSH or WSL workspace `cwd` is a Linux path, so `cwd.join(".git").exists()` at :40 is false and the pane shows "not a git repository" — it looks like the repo is broken rather than like the feature is local-only. Found while building the tickets transport layer, which now does resolve this correctly (see tickets.rs `resolve`). Fix: dispatch on connection the way tickets.rs does — `git -C … rev-parse` over the existing SSH exec channel, or the \\wsl.localhost share for WSL.
- [ ] P3 | 2026-08-12 | app/src-tauri/src/addons.rs:54 | `is_local_workspace` matches `None | Local` only, so a **WSL** workspace is classified as remote and routed down the SSH path, where it dies on "no active SSH session". Affects `insights_fetch` (:813), the docker action (:885) and hygiene (:916). WSL sits in a gap in the frontend predicates too (`types.ts:231-286`: `isRemoteConn` is ssh-only, `isLocalConn` is local-or-null). Fix: treat WSL as local for anything that only needs a filesystem/exec, which is what `local_setup::wsl_exec` already provides.
- [ ] P2 | 2026-08-13 | app/src/browserDevMode.ts (inspectScript → snapshot) | The element screenshot rendered as a fully TRANSPARENT png in the first live smoke — the modal preview was bare checkerboard. The captured element was a `<pre>`, which plausibly has no background of its own, so this may be correct behaviour. Unconfirmed either way. Repro: Dev Mode → right-click something with a visible background colour and text. If that is also blank, `foreignObject` is not drawing at all and the suspects are the `inlineStyles` pass or an `XMLSerializer` output the SVG parser rejects — both of which fail silently to a blank canvas today, with no way for the user to tell "the element really is transparent" from "the snapshot broke".
- [ ] P3 | 2026-08-13 | app/src-tauri/src/tickets.rs (`resolve`, ssh arm) | The tmux pane-cwd rung takes the FIRST session it finds for the workspace (`tmux_session_for_workspace`). With several panes sitting in different directories, the ticket follows whichever session the map happens to yield first, which is not deterministic across runs. Harmless while panes share a project — the observed case had three sessions all in `/home/runner/club` — but it should prefer the ACTIVE pane once that matters.

## Done

- [x] P0 | 2026-08-18 | manifest.json, hooks/*.json, app/src-tauri/cli/src/hooks.rs:152, README.md, docs/RELEASING.md | GitHub repo rename — ALREADY DONE, found by accident. Filed as a blocker on the assumption the repo was still `winmux`; the push to main answered with `remote: This repository moved ... yyhezkel/ymux.git`, i.e. Yossi had already renamed it. Verified rather than assumed: `gh api repos/yyhezkel/ymux` -> full_name=yyhezkel/ymux, default_branch=main; `raw.githubusercontent.com/yyhezkel/ymux/main/hooks/claude-code.json` -> 200, so `load_spec`'s canonical URL resolves; the OLD raw URL also -> 200 via GitHub's redirect, so pre-rename CLIs still fetch the spec. Local `origin` was still on the old URL (working only through the redirect) and was repointed with `git remote set-url`. Note this is the SHARED .git/config — the main checkout and every other worktree pick it up too. `manifest.json` stays un-rewritten on purpose: it describes the published v0.4.5-beta.1 assets, which really are named `winmux_0.4.5_*` and are still served from the old release URLs via the same redirect.

- [x] P2 | 2026-08-11 | app/src/browserDevMode.ts:72 | `saveProjectOverride` had no caller, so rung 1 of the tickets project ladder was unreachable from the UI. FIXED 2026-08-12 by e0c32d6 — a text field + Apply/Use-default in the capture modal's destination row, which refetches the resolution so the new target is visible before saving. Deliberately not a folder browser: Phase 54 was reverted for making the user browse an SFTP tree for something the app derives.
- [x] P3 | 2026-08-11 | app/src/TicketModal.tsx:71 | `screenshot_data_url` was hardcoded null, so no ticket could ever have an image despite a complete Rust decode/write path. FIXED 2026-08-12 by 76ec575 — the element is rasterized in-page via an SVG foreignObject (no library injected), with computed styles inlined because a node cloned into foreignObject inherits nothing from the document's stylesheets. Degrades to null on tainted canvas / oversize / timeout rather than to a wrong ticket. Read back through tickets_screenshot as a data-url so remote tickets work identically.

- [x] P1 | 2026-08-09 | app/src-tauri/src/local_setup.rs:1195 | Elevated `wsl --install` is undiagnosable: `Ok(Ok(code)) => Ok((code, String::new()))` hands `classify_wsl_install` an EMPTY output string, because ShellExecuteEx("runas") gives no stdout/stderr pipe. So on the elevated path the text fallbacks (`contains("elevat")`, `contains("reboot")`) are dead code, and every code that is not 0/3010/1223/740 collapses to `StepFailed { exit_code, stderr: "" }`. HIT LIVE 2026-08-09 on Yossi's box: `wsl --install --no-launch -d Ubuntu` exited 1 after 23s of real work and the UI could only say "exit 1" — no reason, nothing in debug.log. The doc comment calls the lost pipes "an acceptable trade because the codes it does give us are unambiguous", but generic 1 is the common case and is exactly the ambiguous one. Fix: relaunch elevated via `cmd /c "wsl --install ... > %TEMP%\ymux-wsl.log 2>&1"` (or a ShellExecuteEx'd helper that redirects) and read the file back after the wait, so the real message survives. FIXED 2026-08-09 by fd1f537 — elevate `cmd /s /c` and redirect into a per-run temp file (path allowlisted by the new cmd_safe_path, WSL_UTF8=1 so the text is UTF-8), read it back after the wait, delete on every path. Exit code now logged on success too. Quoting verified against a real cmd.exe with --status swapped in for --install: redirect lands, 0 NUL bytes, real output captured. 5 new unit tests.
- [x] P1 | 2026-08-03 | app/src-tauri/src/lib.rs:1565 | emit_data strict-UTF8 decoder: if `leftover` starts with an invalid byte, `valid_up_to==0` returns WITHOUT draining -> that pane goes permanently silent and the buffer grows unbounded (no cap, no error branch). Found while tracing the local-PowerShell Hebrew bug. FIXED 2026-08-03 — decoder moved to `pty_decode::Utf8Stream`, which branches on `Utf8Error::error_len()`: a truncated tail is held (Hebrew/emoji splits still survive), genuinely invalid bytes become U+FFFD and are drained, so a stall is structurally impossible and leftover is capped at 3 bytes by construction. 14 unit tests.
- [x] P2 | 2026-08-03 | app/src-tauri/src/local_setup.rs:1096 | local-setup failures log only `err.user_message()`, dropping `stderr` -> the UI red card shows the real reason but the log never does, so a user-reported install failure is undiagnosable after the fact. FIXED 2026-08-03 by 6753759 — failure logs now append StepFailed stderr.
- [x] P1 | 2026-08-19 | app/src/sidebar.css, app/src/Sidebar.tsx | VERIFIED LIVE by Yossi on the branch build (a0ce0d0, run 32235591934), across three runs. Confirmed working: the colour flood gone and rows uniform in Hebrew/RTL light mode; the badge column straight; row names all starting on one vertical line; the masthead scaling with the rail; double-click on the wordmark collapsing and expanding; Tab-to-wordmark + Enter doing the same. Two real defects were found by running it that the fixture had missed and are now fixed in f6f588f and a0ce0d0 -- see the entry below for the one that is still open.
- [ ] P3 | 2026-08-19 | app/src/sidebar.css | SIDEBAR SURFACES STILL UNEXERCISED after the design pass. Not defects, just never run: (1) Settings -> Appearance -> UI size dragged 11pt..16pt -- the whole point of the px -> --w-fs-* sweep is that group headers, counts and the bottom action row now scale with it too, and only a live slider proves that; (2) drag-reorder of workspaces AND groups, whose CSS all moved file in b16537d; (3) the context menus, inline group rename and the colour picker; (4) 11 of the 13 presets -- app/dev/sidebar-fixture.html carries tokyo-night and modernist only, and themes-redesign.css:407 flattens radii by an explicit class list that any NEW sidebar class will be missing from; (5) prefers-reduced-motion. Cheapest sweep is one `npm run tauri dev` with the font slider and a drag, then flipping presets in Settings.
