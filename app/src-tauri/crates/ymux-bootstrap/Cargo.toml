[package]
name = "ymux-bootstrap"
version = "0.0.0"
edition = "2021"
publish = false
description = "Phase 51.D: deploy the Linux ymux CLI to a remote SSH server (no tauri dep)"

[dependencies]
ymux-core = { path = "../ymux-core" }
russh = "0.49"
russh-sftp = "2"
tokio = { version = "1", features = ["sync", "rt", "io-util", "macros"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
sha2 = "0.10"
